# Roadmap

Store one strategic item per `RM-###-<slug>.md`. Project links are derived from each project spec's optional `roadmap_item` field; do not write project lists into roadmap items.

Use `delano roadmap add`, lifecycle commands, and promotion so contract rules and evidence remain consistent.
